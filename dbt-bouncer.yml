manifest_path: target/manifest.json

checks:

  # --------------------------------------------------------------------------
  # Naming conventions
  # --------------------------------------------------------------------------
  - name: check_model_name_by_folder
    include: ^models/staging
    model_name_pattern: ^stg_

  - name: check_model_name_by_folder
    include: ^models/intermediate
    model_name_pattern: ^int_

  - name: check_model_name_by_folder
    include: ^models/core
    model_name_pattern: ^(fct_|dim_|bridge_)

  - name: check_model_name_by_folder
    include: ^models/marts
    model_name_pattern: ^(fct_|dim_)

  # --------------------------------------------------------------------------
  # Documentation
  # --------------------------------------------------------------------------
  - name: check_source_description_populated
    include: ^models/staging

  # --------------------------------------------------------------------------
  # Testing
  # --------------------------------------------------------------------------
  - name: check_model_has_unique_test
    include: ^models/core

  # --------------------------------------------------------------------------
  # Structure
  # --------------------------------------------------------------------------
  - name: check_model_max_fanout
    include: ^models/
    max_downstream_models: 10

  - name: check_model_has_no_upstream_dependencies
    include: ^models/staging
